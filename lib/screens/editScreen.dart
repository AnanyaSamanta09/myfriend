import 'package:flutter/material.dart';

class EditFriendScreen extends StatelessWidget {
  const EditFriendScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
